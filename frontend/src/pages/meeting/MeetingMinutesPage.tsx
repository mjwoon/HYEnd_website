import styled from 'styled-components';

const Container = styled.div`
  padding: 100px 40px 40px;
  min-height: 100vh;
`;

export default function MeetingMinutesPage() {
  return <Container>회의록 (구현 예정)</Container>;
}
